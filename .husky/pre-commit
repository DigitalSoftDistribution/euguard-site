#!/usr/bin/env bash
. "$(dirname -- "$0")/_/husky.sh" 2>/dev/null || true

# === precommit-typecheck-v1 (auto-installed via install-husky-typecheck.sh) ===
. "$(dirname -- "$0")/_/precommit-typecheck.sh"
